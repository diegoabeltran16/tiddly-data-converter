Docs/refs
Docs/translate.ps1
/rep_export_Windows
/.venv

# ── Extractor HTML (tdc-extractor) ──────────────────────────────────────────
# Build artifacts del crate Rust (nunca versionar)
rust/extractor/target/

# ── Doctor (tdc-doctor) ──────────────────────────────────────────────────────
rust/doctor/target/

# Artefactos de extracción generados en runtime (se regeneran desde el fuente)
data/raw/
data/extraction_output/

# HTML fuente real del usuario — nunca entra al repo como fuente viva.
# Solo se versionan fixtures controlados bajo tests/fixtures/
*.tiddlywiki.html
user_input*.html
data/in/*:Zone.Identifier
# ─────────────────────────────────────────────────────────────────────────────

# Cache local de Python
__pycache__/
*.pyc

/docs/tiddlers_esp

/rep_export_LINUXandMAC
/.vscode

# ── Pipeline S12 — artefactos runtime ────────────────────────────────────────
# Artefactos intermedios y finales del pipeline generados en runtime
/tmp/tdc-pipeline-run/
# Binario Go compilado manualmente (se reconstruye con go build)
go/ingesta/ingest
go/canon/emit
go/canon/canon_preflight
/.codex

/data/in/empty-store.html
/data/in/tiddly-data-converter (Saved).html
/data/in/tiddly-data-converter (Saved).html:Zone.Identifier
/data/out
/data/tmp
/data/sessions
/data/in/objeto_de_estudio_trazabilidad_y_desarrollo.html:Zone.Identifier
data/sessions
